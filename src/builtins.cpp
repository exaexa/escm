#include "builtins.h"

void escm_add_scheme_builtins(scm_env*e)
{

}

/*
 * TODO, specific continuations for all 'special forms':
 * define set!
 * if cond case (and,or)
 * map foreach do
 * let let* letrec
 *
 * (lambda is a syntax!)
 */

